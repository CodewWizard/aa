import { DireflowComponent } from 'direflow-component';
import MaskedInput from './masked-input';

export default DireflowComponent.create({
  component: MaskedInput,
  configuration: {
    tagname: 'masked-input',
  },
  plugins: [],
});